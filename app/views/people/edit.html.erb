<%= bootstrap_form_for(@person) do |f| %>
  <%= f.email_field :email, autocomplete: "email" %>
  <%= f.password_field :password, autocomplete: "password" %>
  <%= f.password_field :mountain_project_user_id, help: "Optional" %>
  <%= f.submit "Save" %>
<% end %>
